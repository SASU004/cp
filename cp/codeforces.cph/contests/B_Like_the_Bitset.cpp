#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solution() {
    int n ,k ;
 vector < int > s(n);
 for(int i=0;i<n;i++)cin>>s[i];
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T = 1; 
    cin >> T; // uncomment if multiple test cases
    while (T--) {
        solution();
    }
    return 0;
}
